import { render } from '@testing-library/react';
import ButtonPagination from '..';

describe('test ButtonPagination', () => {
  it('should render successfully', () => {
    render(<ButtonPagination />);
  });
});
